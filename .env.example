# ============================================
# ENVIRONMENT VARIABLES
# ============================================

# API Backend
VITE_API_BASE_URL=/api

# Sentry (production)
VITE_SENTRY_DSN=

# App Config
VITE_APP_NAME=Health Care Suite
VITE_APP_DEFAULT_LOCALE=es

# OAuth (optional — only needed if backend has OAuth configured)
# No client secrets here — OAuth is server-side on the backend
