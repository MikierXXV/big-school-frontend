/**
 * ============================================
 * CONSTANTS: Routes
 * ============================================
 *
 * Constantes de rutas de la aplicacion.
 */

export const ROUTE_NAMES = {
  HOME: 'home',
  LOGIN: 'login',
  REGISTER: 'register',
  DASHBOARD: 'dashboard',
  NOT_FOUND: 'not-found',
} as const;

export const ROUTE_PATHS = {
  HOME: '/',
  LOGIN: '/login',
  REGISTER: '/register',
  DASHBOARD: '/dashboard',
} as const;
